const fetchQueryStringFromURL = require('./fetchQueryStringFromURL');
const validate = require('./validation');
const sampleMiddleware = require('./sampleMiddleware');
const checkContent_type = require('./checkContent_type')

module.exports = { fetchQueryStringFromURL, validate, sampleMiddleware, checkContent_type }